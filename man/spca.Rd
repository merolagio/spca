% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spca_v1.2_noForce_.R
\name{spca}
\alias{spca}
\title{Computes LS SPCA components}
\usage{
spca(
  M,
  alpha = 0.95,
  ncomps = 0,
  ncomp_byvexp = 0.99,
  method = "c",
  var_selection = c("fwd", "bkw", "step"),
  stop_criterion = c("cvexp", "r2"),
  intensive = FALSE,
  fat_matrix = NULL,
  fixedindex_list = list(),
  centerdata = FALSE,
  scaledata = FALSE,
  PML = FALSE,
  epsPML = 1e-05,
  maxiterPML = 100,
  PMVS = FALSE,
  epsPMVS = 1e-05,
  maxiterPMVS = 200
)
}
\arguments{
\item{M}{Real matrix. The \eqn{n \times p} data matrix or the
\eqn{p \times p} covariance/correlation matrix.}

\item{alpha}{Real in (0,1]. Percentage of variance of the PCs explained by
the sparse component.}

\item{ncomps}{Number of components to compute (default 0, automatic).
Has priority over \code{ncomp_byvexp}.}

\item{ncomp_byvexp}{Real in (0,1]. Stops computating sPCs when the cumulative
vexp reaches this value.}

\item{method}{Character vector. Partial matching enabled.
Allowed values are \code{uSPCA}, \code{cSPCA}, and \code{pSPCA}.
If fewer than \code{ncomps} elements are supplied, the last value is used
for all remaining components.}

\item{var_selection}{Character vector. Only the first letter of the first
element is used: \code{"f"}, \code{"b"}, or \code{"s"}.}

\item{stop_criterion}{Character vector. Only the first letter of the first
element is used: \code{"r"} or \code{"c"}.}

\item{intensive}{Logical. For thin matrices only, requests intensive forward
cvexp selection.}

\item{fat_matrix}{Logical or \code{NULL}. If \code{NULL}, the backend is selected
automatically: data matrices with \eqn{n < p} use the fat backend and all
other inputs use the thin backend. If \code{TRUE}, request the fat-matrix
backend. If \code{FALSE}, use the thin backend, allowing users to force the thin
path for mildly fat matrices.}

\item{fixedindex_list}{List of predetermined indices for components.
Each list element is a vector of 1-based indices. Use \code{NULL} or
\code{integer(0)} for a component with no fixed indices. If the list is
shorter than the number of components to be computed, the remaining
components are treated as unconstrained.}

\item{centerdata}{Logical. Center the columns of \code{M} to zero mean?
Used only when a data matrix is passed.}

\item{scaledata}{Logical. Scale the columns of \code{M}? Used only when a
data matrix is passed.}

\item{PML}{Logical. Use the power method for PCs and sparse-component
eigenvectors.}

\item{epsPML}{Numeric. Tolerance for the PML group.}

\item{maxiterPML}{Integer. Maximum iterations for the PML group.}

\item{PMVS}{Logical. Use the power method in variable selection.}

\item{epsPMVS}{Numeric. Tolerance for the PMVS group.}

\item{maxiterPMVS}{Integer. Maximum iterations for the PMVS group.}
}
\value{
An object of class \code{spca}.
}
\description{
Computes sparse principal components from either a data matrix or a
covariance/correlation matrix. The wrapper routes the computation to the thin or the fat-matrix C++ engine.
}
\details{
Character inputs are parsed transparently by their first letter inside the
main function. Fixed indices are passed through \code{fixedindex_list}, one
list element per component. Different variable selection approaches can be
selected, as explained in the \code{Deatails} section.

For thin matrices, either the covariance/correlation matrix can be passed. In the former case, the covariance will be computed and assigned to a new matrix.
}
\seealso{
Other spca: 
\code{\link{change_loadings_sign_spca}()},
\code{\link{compare_spca}()},
\code{\link{is.spca}()},
\code{\link{new_spca}()},
\code{\link{plot.spca}()},
\code{\link{print.spca}()},
\code{\link{spca_object}},
\code{\link{summary.spca}()}
}
\concept{spca}
