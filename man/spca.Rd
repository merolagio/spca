% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spca_v1.9_basic.R
\name{spca}
\alias{spca}
\title{Compute LS-SPCA components}
\usage{
spca(
  M,
  alpha = 0.95,
  n_comps = NULL,
  ncomp_by_cvexp = NULL,
  method = c("cspca", "uspca", "pspca"),
  var_selection = c("fwd", "bkw", "step"),
  objective = c("r2", "cvexp"),
  intensive = FALSE,
  fat_matrix = NULL,
  fixed_index_list = NULL,
  center_data = FALSE,
  scale_data = FALSE,
  pm_loading = FALSE,
  eps_pm_loading = 1e-04,
  maxiter_pm_loading = 1000,
  pm_varsel = FALSE,
  eps_pm_varsel = 1e-04,
  maxiter_pm_varsel = 500,
  basic_output = FALSE
)
}
\arguments{
\item{M}{A numeric matrix or data frame. If \code{M} is square and symmetric, it
is treated as a covariance/correlation matrix (only for the tall backend).
Otherwise it is treated as an \eqn{n \times p} data matrix.}

\item{alpha}{A number in \eqn{(0, 1]}. Target proportion used by variable
selection.}

\item{n_comps}{\code{NULL} or a nonnegative integer scalar. Number of components
to compute. If \code{NULL}, \code{ncomp_by_cvexp} is used to determine the number of
components.}

\item{ncomp_by_cvexp}{\code{NULL} or a number in \eqn{(0, 1]}. If \code{n_comps} is
\code{NULL}, components are computed until cumulative variance explained reaches
this value. At least one of \code{n_comps} and \code{ncomp_by_cvexp} must be
supplied.}

\item{method}{A character selecting the LS-SPCA variant. Values may
be \code{"cspca"}, \code{"uspca"}, or \code{"pspca"}; only the first letter is used.}

\item{var_selection}{A character vector selecting the variable-selection
algorithm. Values starting with \code{"f"} use forward selection, values
starting with \code{"b"} use backward elimination, and values starting with
\code{"s"} use forward-stepwise selection.}

\item{objective}{A character vector selecting the stopping criterion
for variable selection. Values starting with \code{"r"} use the squared
correlation criterion; values starting with \code{"c"} use cumulative variance
explained.}

\item{intensive}{A logical scalar. If \code{TRUE}, the tall backend uses intensive
forward CVEXP selection. This option is not available for fat matrices.}

\item{fat_matrix}{\code{NULL} or a logical scalar. If \code{NULL}, data matrices with
more columns than rows use the fat backend and all other inputs use the
tall backend. If \code{TRUE}, the fat backend is requested. If \code{FALSE}, the tall
backend is used.}

\item{fixed_index_list}{\code{\link{NULL}} A list of integer-valued vectors giving
fixed 1-based variable indices for selected components or a factor.
Use \code{NULL} for components without fixed indices.}

\item{center_data}{A logical scalar. If \code{TRUE}, center data-matrix columns
before fitting. Ignored when \code{M} is a covariance/correlation matrix.}

\item{scale_data}{A logical scalar. If \code{TRUE}, scale data-matrix columns
before fitting. Ignored when \code{M} is a covariance/correlation matrix.}

\item{pm_loading}{A logical scalar. If \code{TRUE}, use the power method for PC
and sparse-loading eigenvectors.}

\item{eps_pm_loading}{A positive number. Convergence tolerance for
\code{pm_loading}.}

\item{maxiter_pm_loading}{A positive integer. Maximum number of iterations
for \code{pm_loading}.}

\item{pm_varsel}{A logical scalar. If \code{TRUE}, use the power method inside
variable selection.}

\item{eps_pm_varsel}{A positive number. Convergence tolerance for
\code{pm_varsel}.}

\item{maxiter_pm_varsel}{A positive integer. Maximum number of iterations
for \code{pm_varsel}.}
}
\value{
An object of class \code{spca}, or of class \code{spca_basic} when
\code{basic_output = TRUE}.
}
\description{
\code{spca()} computes sparse principal components from a data matrix or from a
covariance/correlation matrix. Data matrices are routed to the tall or fat
C++ backend; covariance/correlation matrices always use the tall backend.
}
\details{
A covariance matrix as input to \code{M} is accepted only for
tall matrices.
Variable selection is controlled by \code{var_selection}, \code{objective}, and
\code{intensive}.

\tabular{lll}{
\code{var_selection} \tab \code{objective} \tab Algorithm \cr
\code{"fwd"} \tab \code{"r2"} \tab Forward selection with squared-correlation stopping \cr
\code{"bkw"} \tab \code{"r2"} \tab Backward elimination with squared-correlation stopping \cr
\code{"step"} \tab \code{"r2"} \tab Forward-stepwise selection with squared-correlation stopping \cr
\code{"fwd"} \tab \code{"cvexp"} \tab Forward selection with CVEXP stopping \cr
\code{"bkw"} \tab \code{"cvexp"} \tab Backward elimination with CVEXP stopping \cr
\code{"step"} \tab \code{"cvexp"} \tab Forward-stepwise selection with CVEXP stopping
.\cr  \code{intensive = TRUE} requires "fwd" \tab \code{"cvexp"} \tab Intensive
forward CVEXP selection. \cr
}

The fat backend currently uses regression-based forward variable selection only: \code{var_selection = "f"}, \code{intensive = FALSE}.
Any another combination supplied will generate an error.

The returned object is documented in \link{spca_object}.
}
\seealso{
Other spca: 
\code{\link{change_loadings_sign_spca}()},
\code{\link{compare_spca}()},
\code{\link{is.spca}()},
\code{\link{new_spca}()},
\code{\link{plot.spca}()},
\code{\link{print.spca}()},
\code{\link{show_contributions_spca}()},
\code{\link{spca_object}},
\code{\link{summary.spca}()}
}
\concept{spca}
