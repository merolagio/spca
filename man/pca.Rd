% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca_v2.R
\name{pca}
\alias{pca}
\title{Compute Principal Components}
\usage{
pca(
  M,
  ncomps = NULL,
  centerdata = FALSE,
  scaledata = FALSE,
  fat_matrix = NULL,
  screeplot = FALSE,
  qq_plot = TRUE,
  nrow_data = NULL,
  neigen_toplot = NULL
)
}
\arguments{
\item{M}{A data matrix, correlation matrix, or covariance matrix.}

\item{ncomps}{Integer. Number of components to retain. If \code{NULL}, all
components are retained.}

\item{centerdata}{Logical. If \code{TRUE}, center variables to zero mean. If \code{M}
is detected as a data matrix and any column mean is nonzero, centering is
performed automatically.}

\item{scaledata}{Logical. If \code{TRUE}, scale variables.}

\item{fat_matrix}{Logical or \code{NULL}. If \code{NULL}, the backend is selected
automatically: data matrices with \eqn{n < p} use the fat backend and all
other inputs use the tall backend. If \code{TRUE}, request the fat backend. If
\code{FALSE}, use the tall backend. Covariance/correlation matrices always use
the tall backend.}

\item{screeplot}{Logical. If \code{TRUE}, produce a scree plot.}

\item{qq_plot}{Logical. If \code{TRUE}, produce a Wachter QQ-plot with
\link{wachter_qqplot}. The fitted line is based on \code{ncomps}.}

\item{nrow_data}{Integer. Number of rows in the original data set.
Required when \code{qq_plot = TRUE} and \code{M} is a covariance or correlation
matrix. If not available, the Wachter QQ-plot cannot be produced.}

\item{neigen_toplot}{Integer. Number of eigenvalues to show in diagnostic
plots. If \code{NULL}, all eigenvalues are shown.}
}
\value{
An  \link{spca_object} with the addition of the vector \code{eigenvalues}
containing the eigenvalues up to rank of M.
}
\description{
Creates PCA output as an \code{spca} object, so the result can be used with
\code{spca} methods.
}
\details{
\code{ncomps} controls how many components are retained in the returned object.
For the tall backend, setting it below the maximum does not reduce the cost
of the eigendecomposition. For fat data matrices, the fat backend computes
the eigendecomposition in row space and converts the retained eigenvectors
back to variable loadings.
}
\examples{
\dontrun{
data(holzinger)
ho_pca <- pca(hom, ncomps = 4, screeplot = TRUE, nrow_data = 144, qq_plot = TRUE)
summary(ho_pca)
}

}
\seealso{
Other pca: 
\code{\link{spca_screeplot}()},
\code{\link{wachter_qqplot}()}
}
\concept{pca}
