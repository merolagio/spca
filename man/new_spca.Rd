% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_and_validation.R
\name{new_spca}
\alias{new_spca}
\title{Construct an spca object from a set of loadings}
\usage{
new_spca(A, S = NULL, X = NULL, method_name = NULL)
}
\arguments{
\item{A}{A numeric matrix of loadings.}

\item{S}{A numeric covariance or correlation matrix (default \code{NULL}).
If \code{NULL}, \code{X} is used to estimate the covariance matrix.}

\item{X}{A numeric data matrix or data frame (default \code{NULL}). Used to
compute \code{S} when \code{S = NULL}, and to compute scores when supplied.
At least one of \code{S} or \code{X} must be provided.}

\item{method_name}{A character scalar or \code{NULL} (default \code{NULL}).
Name of the method used to compute the loadings.}
}
\value{
An \code{spca} object.
}
\description{
Build an object of class \code{spca} from a loadings matrix and either a
covariance or correlation matrix, a data matrix, or both.
}
\examples{
set.seed(1)
A = round(matrix(runif(24, -1, 1), 12))
A[abs(A) < 0.4] = 0 #no need to scale to unit norm
data(holzinger)
spca_new = new_spca(A, X = holzinger)
is.spca(spca_new)
summary(spca_new)

}
\seealso{
Other spca: 
\code{\link{change_loadings_sign_spca}()},
\code{\link{compare_spca}()},
\code{\link{is.spca}()},
\code{\link{plot.spca}()},
\code{\link{print.spca}()},
\code{\link{show_contributions_spca}()},
\code{\link{spca}()},
\code{\link{spca_object}},
\code{\link{summary.spca}()}
}
\concept{spca}
