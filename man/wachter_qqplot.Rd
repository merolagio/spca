% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{wachter_qqplot}
\alias{wachter_qqplot}
\title{Wachter QQ plot for eigenvalues}
\usage{
wachter_qqplot(
  eigenvalues,
  p = NULL,
  n,
  gamma,
  cor = TRUE,
  common_var = 1,
  nplot = NULL,
  n_fitline = NULL,
  addtitle = TRUE,
  show_plot = TRUE,
  return_plot = FALSE
)
}
\arguments{
\item{eigenvalues}{A numeric vector of eigenvalues, assumed to be sorted in
decreasing order.}

\item{p}{An integer scalar or \code{NULL} (default \code{NULL}). Number of
variables. If \code{NULL}, \code{length(eigenvalues)} is used.}

\item{n}{An integer scalar. Sample size.}

\item{gamma}{A numeric scalar. Aspect ratio. If missing, \code{n / p} is
used.}

\item{cor}{A logical value (default \code{TRUE}). Currently accepted for
compatibility; the plotted quantiles are controlled by \code{common_var}.}

\item{common_var}{A positive numeric scalar (default \code{1}). Common
variance of the variables. Use this when the variables were rescaled to
unit variance. See Details.}

\item{nplot}{An integer scalar or \code{NULL} (default \code{NULL}). Number
of leading eigenvalues to include. If \code{NULL}, all eigenvalues are
included.}

\item{n_fitline}{An integer scalar or \code{NULL} (default \code{NULL}). If
positive, fit a least-squares line using the last \code{n_fitline} points.
If negative, exclude the largest \code{abs(n_fitline)} values from the
fitted line.}

\item{addtitle}{A logical value (default \code{TRUE}). If \code{TRUE}, add a
plot title.}

\item{show_plot}{A logical value (default \code{TRUE}). If \code{TRUE},
print the plot.}

\item{return_plot}{A logical value (default \code{FALSE}). If \code{TRUE},
return the ggplot object.}
}
\value{
If \code{return_plot = TRUE}, returns a \code{ggplot} object.
Otherwise, returns \code{NULL} invisibly.
}
\description{
Produce a QQ plot comparing observed eigenvalues with Marchenko--Pastur
(Wachter) theoretical quantiles.
}
\details{
The QQ plot is based on the Marchenko--Pastur distribution of the
eigenvalues of a random covariance matrix generated from variables with a
common variance. If the data set or covariance matrix comes from variables
with different variances, the QQ plot is not valid. A simple introduction to
the QQ plot can be found at \url{https://brainder.org/tag/wachter-test/};
see the extended vignette for references.
}
\examples{
data(holzinger)
ho_pca = pca(holzinger,  qq_plot = FALSE)
 wachter_qqplot(ho_pca$eigenvalues, p = ncol(holzinger), n = nrow(holzinger),
  cor = TRUE, n_fitline = -3)
}
\seealso{
Other pca: 
\code{\link{pca}()},
\code{\link{spca_screeplot}()}
}
\concept{pca}
